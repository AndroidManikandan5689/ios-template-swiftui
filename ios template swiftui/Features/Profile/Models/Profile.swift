import Foundation
import SwiftUI
import Combine

// For now, reuse the `User` model for profile display. If a separate profile model is needed
// later we can map fields accordingly.
public typealias Profile = User
